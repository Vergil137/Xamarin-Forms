<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:ic="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
			 x:Class="Instagramm.UserProfile"
			 Title="{Binding Name}"
			 Padding="20">
	<ContentPage.Content>
		<StackLayout>
			<Label Text="{Binding Name}" FontSize="Title" FontAttributes="Bold" TextColor="Black"/>
			<ic:CircleImage Source="{Binding ImageUrl}" WidthRequest="60" HeightRequest="60" Aspect="AspectFit" HorizontalOptions="Start"/>
			<Label Text="{Binding Description}" FontSize="Default" TextColor="Gray"/>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>